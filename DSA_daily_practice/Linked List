//Hackerrank-For each node, print its data value on a new line
static void printLinkedList(SinglyLinkedListNode head) {
        SinglyLinkedListNode temp=head;
        while(temp!=null){
            System.out.println(temp.data);
            temp=temp.next;
        }
}
//Hackerrank-Delete the node
public static SinglyLinkedListNode deleteNode(SinglyLinkedListNode llist, int position) {
    // Write your code here
    if(llist==null)return llist;
    if(position==1){
        SinglyLinkedListNode temp=llist;
        llist=llist.next;
        return llist;
    }
    int count=0;
    SinglyLinkedListNode temp=llist;
    SinglyLinkedListNode prev=null;
    while(temp!=null){
        count++;
        if(count==position){
            prev.next.next=prev.next.next.next;
            break;
        }
        prev=temp;
        temp=temp.next;
    }
    return llist;
    
    }

}
//Hackerrank-insert at first
static SinglyLinkedListNode insertNodeAtHead(SinglyLinkedListNode llist, int data) {
        SinglyLinkedListNode temp=new SinglyLinkedListNode(data);
        temp.next=llist;
        return temp;

    }
//Hackerrank-insert at end
static SinglyLinkedListNode insertNodeAtTail(SinglyLinkedListNode head, int data) {
        SinglyLinkedListNode last=new SinglyLinkedListNode(data);
        if(head==null)return last;
        SinglyLinkedListNode temp=head;
        while(temp.next!=null){
            temp=temp.next;
        }
        temp.next=last;
        return head;


    }
//Hackerrank-insert at specific position
public static SinglyLinkedListNode insertNodeAtPosition(SinglyLinkedListNode llist, int data, int position) {
    // Create new node
    SinglyLinkedListNode insert = new SinglyLinkedListNode(data);

    // Case: insert at head
    if (position == 0) {
        insert.next = llist;
        return insert;
    }

    // Traverse to the node before the position
    SinglyLinkedListNode temp = llist;
    int count = 0;
    while (temp != null) {
        if (count == position - 1) {
            insert.next = temp.next;
            temp.next = insert;
            break; // insertion done
        }
        temp = temp.next;
        count++;
    }

    return llist;
    
}
@Doubly Linked List
//hackerrank-inserting a node into sorted linkedlist
public static DoublyLinkedListNode sortedInsert(DoublyLinkedListNode llist, int data) {
    DoublyLinkedListNode newNode = new DoublyLinkedListNode(data);

    // Case 1: Empty list
    if (llist == null) {
        return newNode;
    }

    // Case 2: Insert at head
    if (data < llist.data) {
        newNode.next = llist;
        llist.prev = newNode;
        return newNode;
    }

    DoublyLinkedListNode temp = llist;

    // Traverse until we find the correct position
    while (temp.next != null && temp.next.data < data) {
        temp = temp.next;
    }

    // Insert after temp
    newNode.next = temp.next;
    newNode.prev = temp;
    temp.next = newNode;

    if (newNode.next != null) {
        newNode.next.prev = newNode;
    }

    return llist;
}
//
